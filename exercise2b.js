//Creates an arrow function with myVal as a parameter
let message = (myVal)=>{

    // Displays the Value of myVal in the console
    console.log(myVal);
}

// Calls the function and passes "Hello World!" as the value message (Hello World)
message("Hello World!")
